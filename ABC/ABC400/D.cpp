#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    int H,W;
    cin >> H >> W;
    vector<string> S(H);
    for(int i = 0; i < H; i++) cin >> S[i];
    int si,sj,gi,gj;
    cin >> si >> sj >> gi >> gj;
    si--; sj--; gi--; gj--;

    /*
        とりあえずbfsだ
    */
}
