#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    int N;
    string S;
    cin >> N >> S;

    vector<int> pos(N);
    for(int i = 0; i < N; i++){
        pos[i] = i;
    }

    do{
        
    }while(next_permutation(pos.begin(),pos.end()));
}
